{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "generate": {
      "inputs": ["prisma/schema.prisma"],
      "outputs": ["prisma/.prisma/client"]
    }
  }
}
